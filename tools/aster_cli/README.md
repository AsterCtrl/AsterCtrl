# xrobot-tools

Schema validation, dependency resolution, deployment planning, and deterministic
code generation for the control-2027 framework. This repository is independent
from the upstream XRobot source tree.

The command-line entry point is `xrctl`.
