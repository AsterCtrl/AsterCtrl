api_version: aster.dev/v1alpha2
kind: Hardware
metadata:
  name: linux-usb-tty
spec:
  platform: linux
  resources:
    usb_console:
      kind: usb_cdc
      backend: tty
      device: /dev/ttyACM0
      options: {}
